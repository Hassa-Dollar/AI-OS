# components/web — environment TEMPLATE. Copy to .env (gitignored) and fill.
# Only PUBLIC values live here; Vite exposes only VITE_* to the client bundle.

VITE_API_URL=http://localhost:8787
# Publishable, NOT secret. Never put the Stripe secret key here.
VITE_STRIPE_PUBLISHABLE_KEY=
